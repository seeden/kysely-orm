import Database from './Database';

export default class Base {
  constructor() {
    
  }
  /*
  static db;
  static table<TableName>;

  db: Database<DB>;
  table: TableName;

  constructor(db: Database<DB>, table: TableName) {
    this.db = db;
    this.table = table;
  }
  */
}
